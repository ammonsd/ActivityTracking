# Environment Variables Template for Task Activity Management System
# Copy this file to .env and update with your actual values
# The .env file is automatically ignored by git for security

# Database Configuration
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password_here

# Optional: Override default database URL
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/AmmoP1DB

# Optional: Set active Spring profile
# SPRING_PROFILES_ACTIVE=local

# Production Notes:
# - Use strong passwords (minimum 12 characters, mixed case, numbers, symbols)
# - Never commit actual credentials to version control
# - Consider using password managers for credential generation and storage
# - For production deployments, use proper secrets management systems