# Task Activity Management - Environment Variables
# Copy this file to .env and set appropriate values
# CRITICAL: Never commit .env to version control
#
# SECURITY WARNING: Environment variables are visible via 'docker inspect' and 'docker exec'
# This file is for LOCAL DEVELOPMENT ONLY
# Production deployments MUST use Docker secrets or AWS Secrets Manager

# ==============================================================================
# CRITICAL SECURITY REQUIREMENTS
# ==============================================================================
# The application will FAIL TO START if these are not properly configured

# JWT Secret Key (REQUIRED)
# Must be at least 256 bits (32 bytes)
# Generate: openssl rand -base64 32
# Or PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
JWT_SECRET=

# Admin Initial Password (REQUIRED for production)
# Minimum requirements:
# - At least 12 characters
# - Include uppercase, lowercase, number, and special character
# - Will be forced to change on first login
APP_ADMIN_INITIAL_PASSWORD=

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# Database credentials
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password_here

# Optional: Override default database URL
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/AmmoP1DB

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================

# Optional: Set active Spring profile
# Options: local, docker, aws
# SPRING_PROFILES_ACTIVE=docker

# ==============================================================================
# LOGGING CONFIGURATION
# ==============================================================================

# Enable/disable file logging (true/false)
# ENABLE_FILE_LOGGING=true

# Log file path
# Linux/Mac: /var/log/app
# Windows: C:/Logs
# WSL2: /mnt/c/Logs
# LOG_PATH=/var/log/app

# ==============================================================================
# OPTIONAL: EMAIL CONFIGURATION
# ==============================================================================

# Enable email notifications (true/false)
# MAIL_ENABLED=true

# Use AWS SES SDK (recommended for production)
# MAIL_USE_AWS_SDK=true

# Email from address
# MAIL_FROM=noreply@yourdomain.com

# Admin email for notifications
# ADMIN_EMAIL=admin@yourdomain.com

# Expense approvers (comma-separated)
# EXPENSE_APPROVERS=approver1@yourdomain.com,approver2@yourdomain.com

# ==============================================================================
# OPTIONAL: AWS CONFIGURATION (for AWS deployments)
# ==============================================================================

# AWS Region
# AWS_REGION=us-east-1

# S3 bucket for receipt storage
# STORAGE_S3_BUCKET=taskactivity-receipts-prod

# CORS allowed origins (comma-separated)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ==============================================================================
# DEPLOYMENT NOTES
# ==============================================================================
#
# Local Development:
#   1. Generate JWT_SECRET: openssl rand -base64 32
#   2. Set APP_ADMIN_INITIAL_PASSWORD to a secure password
#   3. Copy .env.example to .env and fill in values
#   4. Run: docker compose --profile host-db up -d
#
# AWS/Production:
#   1. Store JWT_SECRET in AWS Secrets Manager: taskactivity/jwt/secret
#   2. Store APP_ADMIN_INITIAL_PASSWORD in AWS Secrets Manager: taskactivity/admin/credentials
#   3. Never use default values in production
#   4. See aws/AWS_Deployment.md for complete setup guide
#
# Security Best Practices:
#   - Never commit .env to version control (already in .gitignore)
#   - Use different JWT secrets for each environment
#   - Rotate JWT secrets regularly in production
#   - Use AWS Secrets Manager or similar for production secrets
#   - Enable MFA on AWS accounts with secret access
#   - Review IAM policies regularly